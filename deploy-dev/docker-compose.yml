networks:
  matrix_cloud:
    name: matrix_cloud
    driver: bridge

services:
  #https://github.com/nacos-group/nacos-docker
  nacos:
    image: nacos/nacos-server:${NACOS_VERSION}
    container_name: nacos
    env_file:
      - ./env/nacos-standlone-mysql.env
    volumes:
      - ../logs/nacos:/home/nacos/logs
    ports:
      - "8080:8080"
      - "8848:8848"
      - "9848:9848"
      - "9555:9555"
    depends_on:
      mysql:
        condition: service_healthy
    restart: always
    networks:
      - matrix_cloud
    # 资源限制
    mem_limit: 256m
    mem_reservation: 128m
    cpus: 1.0
    # 健康检查 - 增加启动等待时间，让Nacos有足够时间初始化
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8848/nacos/actuator/health" ]
      interval: 10s
      timeout: 10s
      retries: 5
      start_period: 30s  # Nacos 启动较慢，建议加长


  mysql:
    container_name: mysql8
    image: mysql:${MYSQL_VERSION}
    env_file:
      - ./env/mysql.env
    volumes:
      - ../mysql_data:/var/lib/mysql
      #https://raw.githubusercontent.com/alibaba/nacos/3.2.2/distribution/conf/mysql-schema.sql
      #https://github.com/xuxueli/xxl-job/blob/master/doc/db/tables_xxl_job.sql
      #https://github.com/apache/incubator-seata/blob/2.x/script/server/db/mysql.sql
      - ./mysql-init:/docker-entrypoint-initdb.d/
    command:
      --character-set-server=utf8mb4
      --collation-server=utf8mb4_unicode_ci
    ports:
      - "3306:3306"
    restart: always
    networks:
      - matrix_cloud
    # 资源限制
    mem_limit: 256m
    mem_reservation: 128m
    cpus: 1.0
    # 健康检查
    healthcheck:
      test: [ "CMD", "mysqladmin" ,"ping", "-h", "localhost" ]
      interval: 10s
      timeout: 10s
      retries: 5
      start_period: 30s

  redis:
    image: redis:${REDIS_VERSION}
    container_name: redis
    ports:
      - "6379:6379"
    environment:
      # 时区上海
      TZ: Asia/Shanghai
    volumes:
      # 配置文件
      - ./redis/conf:/redis/config
      # 数据文件
      - ./redis/data/:/redis/data/
    command: "redis-server /redis/config/redis.conf"
    privileged: true
    restart: always
    networks:
      - matrix_cloud
    # 资源限制
    mem_limit: 256m
    mem_reservation: 128m
    cpus: 0.5


  # https://github.com/lk0423/sentinel-dashboard
  sentinel:
    image: linking0606/sentinel-dashboard:${SENTINEL_VERSION}
    container_name: sentinel
    environment:
      PARAM: -Dserver.port=8088 -Dcsp.sentinel.dashboard.server=localhost:8088 -Dproject.name=sentinel-dashboard -Ddatasource.provider=nacos -Ddatasource.provider.nacos.server-addr=nacos:8848 -Ddatasource.provider.nacos.username=nacos -Ddatasource.provider.nacos.password=nacos -Ddatasource.provider.nacos.namespace=dev
      TZ: Asia/Shanghai
    ports:
      - "8088:8088"
    restart: always
    depends_on:
      nacos:
        condition: service_healthy
    networks:
      - matrix_cloud
    # 资源限制
    mem_limit: 256m
    mem_reservation: 128m
    cpus: 0.5
    # 健康检查
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8088" ]
      interval: 10s
      timeout: 10s
      retries: 3
      start_period: 30s


  xxl-job-admin:
    image: xuxueli/xxl-job-admin:${XXL_JOB_VERSION}
    container_name: xxl-job-admin
    volumes:
      - ../logs/xxl-job:/data/applogs
      - ./xxl-job/application.properties:/application.properties
    environment:
      TZ: Asia/Shanghai
      PARAMS: --spring.config.location=/application.properties
      JAVA_OPTS: -Xmx512m
    ports:
      - 8090:8080
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - matrix_cloud
    restart: always
    # 资源限制
    mem_limit: 256m
    mem_reservation: 128m
    cpus: 0.5
    # 健康检查
    healthcheck:
      test: [ "CMD-SHELL", "find /proc -name 'cmdline' -exec cat {} \\; 2>/dev/null | grep -q 'java'" ]
      interval: 10s
      timeout: 10s
      retries: 5
      start_period: 60s


  # Seata 2.6.0 NamingServer(control-plane)：独立注册中心 + 控制台 UI
  # Seata 2.6.0 把 console 从 server 移出，且捆绑的 nacos-client 1.4.6 用 v1 naming API(已被 Nacos 3.x 移除)，
  # 故 seata-server 改用自研 namingserver 注册，不再依赖 nacos naming。
  seata-naming-server:
    image: apache/seata-naming-server:${SEATA_NAMING_VERSION}
    container_name: seata-naming-server
    environment:
      TZ: Asia/Shanghai
      # namingserver 注册端点需鉴权，默认 console user 为空 → 注册返回 401。
      # 用 Spring Boot 标准机制注入 console 用户(seata/seata)，与 seata-server registry 账号一致。
      SPRING_APPLICATION_JSON: '{"console":{"user":{"username":"seata","password":"seata"}},"seata":{"namingserver":{"auth":{"enabled":false}}}}'
    ports:
      - "8081:8081"
    restart: always
    networks:
      - matrix_cloud
    mem_limit: 256m
    mem_reservation: 128m
    cpus: 0.5
    healthcheck:
      # jib 镜像无 curl/wget，用 bash /dev/tcp 做 TCP 探活
      test: [ "CMD-SHELL", "bash -c '</dev/tcp/localhost/8081'" ]
      interval: 10s
      timeout: 10s
      retries: 5
      start_period: 30s

  seata-server:
    image: apache/seata-server:${SEATA_VERSION}
    container_name: seata-server
    env_file:
      - ./env/seata.env
    ports:
      - "8091:8091"
    volumes:
      - ./seata/application.yml:/seata-server/resources/application.yml
      - ./seata/jdbc/mysql.jar:/seata-server/libs/mysql.jar
    restart: always
    privileged: true
    depends_on:
      seata-naming-server:
        condition: service_healthy
      mysql:
        condition: service_healthy
    networks:
      - matrix_cloud
    # 资源限制
    mem_limit: 256m
    mem_reservation: 128m
    cpus: 0.5
    # 健康检查：2.6.0 server 为非 web 模式(web-application-type:none)，8091 是 Netty RPC 端口，用 TCP 探活
    healthcheck:
      test: [ "CMD-SHELL", "bash -c '</dev/tcp/localhost/8091'" ]
      interval: 10s
      timeout: 10s
      retries: 5
      start_period: 40s



#
#
#  mqnamesrv:
#    image: apache/rocketmq:${ROCKETMQ_VERSION}
#    container_name: mqnamesrv
#    environment:
#      JAVA_OPT: -server -Xms512m -Xmx512m
#    ports:
#      - "9876:9876"
#    volumes:
#      - ./rocketmq/data/logs:/home/rocketmq/logs
#    command: sh mqnamesrv
#    restart: always
#    networks:
#      - matrix_cloud
#    # 资源限制
#    mem_limit: 256m
#    mem_reservation: 128m
#    cpus: 0.5
#    # 健康检查 - 修复健康检查命令，使用更可靠的检查方式
#    healthcheck:
#      test: [ "CMD", "sh", "-c", "ps aux | grep mqnamesrv | grep -v grep" ]
#      interval: 30s
#      timeout: 10s
#      retries: 3
#      start_period: 60s
#
#
#  mqbroker1:
#    image: apache/rocketmq:${ROCKETMQ_VERSION}
#    container_name: mqbroker1
#    ports:
#      - "10911:10911"
#      - "10909:10909"
#      - "10912:10912"
#    environment:
#      JAVA_OPT_EXT: -server -Xms512M -Xmx512M -Xmn256m
#    volumes:
#      - ./rocketmq/broker1/conf/broker.conf:/home/rocketmq/rocketmq-${ROCKETMQ_VERSION}/conf/broker.conf
#      - ./rocketmq/broker1/logs:/home/rocketmq/logs/rocketmqlogs
#      - ./rocketmq/broker1/store:/home/rocketmq/store
#    command: sh mqbroker -c /home/rocketmq/rocketmq-${ROCKETMQ_VERSION}/conf/broker.conf
#    depends_on:
#      mqnamesrv:
#        condition: service_healthy
#    restart: always
#    networks:
#      - matrix_cloud
#    # 资源限制
#    mem_limit: 256m
#    mem_reservation: 128m
#    cpus: 1.0
#    # 健康检查
#    healthcheck:
#      test: [ "CMD", "sh", "-c", "echo \"status\" | nc localhost 10911" ]
#      interval: 30s
#      timeout: 10s
#      retries: 3
#      start_period: 60s
#    # 可扩展性配置
#    deploy:
#      # 支持Docker Swarm模式下的扩展
#      replicas: 1
#      # 资源限制
#      resources:
#        limits:
#          cpus: '1.0'
#          memory: 2g
#        reservations:
#          cpus: '0.5'
#          memory: 1g
#      # 重启策略
#      restart_policy:
#        condition: on-failure
#        delay: 5s
#        max_attempts: 3
#        window: 120s
#      # 服务更新配置
#      update_config:
#        parallelism: 1
#        delay: 10s
#        failure_action: rollback
#      # 服务回滚配置
#      rollback_config:
#        parallelism: 1
#        delay: 10s
#    # 服务标签，便于管理和监控
#    labels:
#      - "com.matrix-cloud.service=rocketmq-broker"
#      - "com.matrix-cloud.environment=dev"
#      - "com.matrix-cloud.version=${ROCKETMQ_VERSION}"
#
#  mqconsole:
#    image: styletang/rocketmq-console-ng:latest
#    container_name: mqconsole
#    ports:
#      - "19876:19876"
#    environment:
#      JAVA_OPTS: -Dserver.port=19876 -Drocketmq.namesrv.addr=mqnamesrv:9876 -Dcom.rocketmq.sendMessageWithVIPChannel=false
#    depends_on:
#      mqnamesrv:
#        condition: service_healthy
#    networks:
#      - matrix_cloud
#    restart: always
#    # 资源限制
#    mem_limit: 256m
#    mem_reservation: 128m
#    cpus: 0.5
#    # 健康检查
#    healthcheck:
#      test: [ "CMD", "curl", "-f", "http://localhost:19876" ]
#      interval: 30s
#      timeout: 10s
#      retries: 3
#      start_period: 60s
