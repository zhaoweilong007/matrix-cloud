server:
  port: 9001

spring:
  application:
    name: matrix-gateway
  profiles:
    active: ${PROFILE:dev}
logging:
  # 配置logstash tcp地址
  stash:
    address: ${LOGSTASH_ADDRESS:localhost:5000}
  level:
    com.matrix.mapper: debug
    com.matrix.api.**: debug
  file:
    path: /var/logs