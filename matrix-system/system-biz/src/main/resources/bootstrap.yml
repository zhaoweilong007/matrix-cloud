server:
  port: 9004

spring:
  application:
    name: matrix-system
  profiles:
    active: ${PROFILE:dev}
  config:
    import:
      - optional:nacos:env.properties
      - optional:nacos:application-common.yml
      - optional:nacos:datasource.yml
