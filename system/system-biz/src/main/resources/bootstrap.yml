server:
  port: 9001

spring:
  application:
    name: system-server
  profiles:
    include: common
    active: dev

logging:
  file:
    path: /var/logs
  level:
    com.matrix.mapper: debug
    com.matrix.api.**: debug