// 应用模块（产出 Docker 镜像）- 从根 build.gradle 中获取
def microservices = rootProject.ext.appModules

// Jib 配置参数
def registryUrl = ''
def registryNamespace = ''
def registryUsername = ''
def registryPassword = ''
def baseImage = 'amazoncorretto:21'

println "build services: ${microservices*.name}"
println "build profile: ${PROFILE}"

File agentFile = file('./skywalking-agent')

for (def proj : microservices) {
    proj.plugins.apply('com.google.cloud.tools.jib')
    proj.jib {
        from {
            image = baseImage
        }
        to {
            image = "${registryUrl}${registryNamespace}${proj.name}:${proj.version}"
            auth {
                username = registryUsername
                password = registryPassword
            }
            tags = ['latest', "${proj.version}"]
        }
        allowInsecureRegistries = true
        container {
            environment = [
                SW_AGENT_NAME: proj.name.toString(),
                JAVA_TOOL_OPTIONS: ''
            ]
            // JDK 21 优化参数
            def baseJvmFlags = [
                '-Djava.security.egd=file:/dev/./urandom',
                '-Dfile.encoding=utf-8',
                '-Djdk.virtualThreadScheduler.parallelism=10',
                '-Djdk.virtualThreadScheduler.maxPoolSize=256',
                '-XX:+UseZGC',
                '-XX:+ZGenerational',
                '-XX:+UseStringDeduplication',
                '-XX:+OptimizeStringConcat',
                '-Djava.nio.preferDirectBuffers=true',
                '-Dmanagement.endpoint.health.enabled=true',
                '-Dmanagement.endpoints.web.exposure.include=health,info,metrics'
            ]
            jvmFlags = baseJvmFlags + ['-javaagent:/skywalking-agent/skywalking-agent.jar']
            creationTime = 'USE_CURRENT_TIMESTAMP'
            volumes = ['/skywalking/agent']
            ports = ['8080', '9090']
        }

        extraDirectories {
            paths {
                path {
                    from = agentFile
                    into = '/skywalking-agent'
                }
            }
        }
    }
}
